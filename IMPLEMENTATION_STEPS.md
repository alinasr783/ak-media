# خطوات التطبيق - Implementation Steps

## 🚀 خطوات تفعيل الميزات الجديدة

### المرحلة 1️⃣: تحديث قاعدة البيانات (Supabase)

#### الخطوة 1: فتح Supabase Dashboard
1. افتح [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **SQL Editor**

#### الخطوة 2: تشغيل Migration
1. انسخ محتوى الملف `database-migration-annual-billing.sql`
2. الصقه في SQL Editor
3. اضغط **Run**

```sql
-- Migration: Add annual billing support
ALTER TABLE subscriptions 
ADD COLUMN IF NOT EXISTS billing_period TEXT DEFAULT 'monthly' 
CHECK (billing_period IN ('monthly', 'annual'));

ALTER TABLE subscriptions 
ADD COLUMN IF NOT EXISTS amount DECIMAL(10, 2);

UPDATE subscriptions 
SET billing_period = 'monthly' 
WHERE billing_period IS NULL;

CREATE INDEX IF NOT EXISTS idx_subscriptions_billing_period 
ON subscriptions(billing_period);
```

#### الخطوة 3: التحقق من النجاح
```sql
-- تحقق من وجود الأعمدة الجديدة
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'subscriptions';
```

يجب أن ترى:
- ✅ `billing_period` (text)
- ✅ `amount` (numeric)

---

### المرحلة 2️⃣: اختبار الكود محلياً

#### الخطوة 1: تشغيل Development Server
```bash
npm run dev
```

#### الخطوة 2: اختبار صفحة الأسعار
1. افتح `http://localhost:5173`
2. اذهب لقسم "الأسعار"
3. **اختبر Toggle:**
   - اضغط "شهري" → يجب أن ترى الأسعار الشهرية
   - اضغط "سنوي" → يجب أن ترى الأسعار السنوية
   - تأكد من ظهور badge "وفر شهرين"
   - تأكد من ظهور badges التوفير على كل باقة

#### الخطوة 3: اختبار صفحة تأكيد الخطة
1. اختر أي باقة
2. في صفحة التأكيد:
   - **Toggle شهري/سنوي:**
     - اضغط "شهري" → السعر الشهري
     - اضغط "سنوي" → السعر السنوي (10 أشهر)
   - **تحقق من الحسابات:**
     - المثال: باقة 800 ج/شهر
     - شهري: 800 ج لشهر واحد
     - سنوي: 8,000 ج لـ 12 شهر (10 أشهر × 800)
   - **بطاقة العرض الخاص:**
     - يجب أن تظهر عند اختيار "سنوي"
     - خلفية خضراء gradient
     - أيقونة ⚡
   - **تفاصيل الدفع:**
     - سعر الباقة
     - المدة
     - خصم السنوي
     - المجموع + السعر الشهري المكافئ

#### الخطوة 4: اختبار كود الخصم
1. أدخل كود خصم
2. تحقق من تطبيقه على السعر السنوي بشكل صحيح
3. المثال:
   - سعر سنوي: 8,000 ج
   - كود خصم 10%: -800 ج
   - المجموع: 7,200 ج

#### الخطوة 5: اختبار الاشتراك الفعلي
1. أكمل عملية الاشتراك
2. **تحقق من قاعدة البيانات:**
```sql
SELECT 
  id,
  plan_id,
  billing_period,
  amount,
  current_period_start,
  current_period_end,
  status
FROM subscriptions
ORDER BY created_at DESC
LIMIT 5;
```

3. **تحقق من البيانات:**
   - ✅ `billing_period` = 'annual' أو 'monthly'
   - ✅ `amount` = السعر الصحيح
   - ✅ `current_period_end` = سنة من الآن (للسنوي) أو شهر (للشهري)

---

### المرحلة 3️⃣: اختبار الحالات الخاصة

#### Test Case 1: الباقة المجانية
```
السيناريو: اختيار الباقة المجانية
التوقع: 
  - لا يظهر toggle السنوي
  - السعر = 0 ج
  - النص = "مجاني"
```

#### Test Case 2: التبديل بين الفترات
```
السيناريو: تبديل بين شهري وسنوي عدة مرات
التوقع:
  - تحديث فوري للأسعار
  - لا يوجد lag أو errors
  - الحسابات صحيحة دائماً
```

#### Test Case 3: كود الخصم + الاشتراك السنوي
```
السيناريو: تطبيق كود خصم على اشتراك سنوي
الحساب:
  1. سعر شهري: 800 ج
  2. سعر سنوي: 800 × 10 = 8,000 ج
  3. كود خصم 10%: 8,000 × 0.1 = 800 ج
  4. المجموع: 8,000 - 800 = 7,200 ج
```

#### Test Case 4: Dark Mode
```
السيناريو: تبديل إلى Dark Mode
التوقع:
  - جميع الألوان تتكيف
  - التباين واضح
  - لا يوجد مشاكل في القراءة
```

#### Test Case 5: Responsive Design
```
السيناريو: فتح على أجهزة مختلفة
الأجهزة:
  - Desktop (1920px)
  - Tablet (768px)
  - Mobile (375px)
التوقع:
  - Layout يتكيف
  - Toggle يعمل بسلاسة
  - النصوص مقروءة
  - الأزرار clickable
```

---

### المرحلة 4️⃣: Performance Testing

#### Test 1: Re-render Count
استخدم React DevTools:
```
1. افتح React DevTools
2. فعّل "Highlight updates"
3. بدّل بين شهري/سنوي
4. التوقع: فقط المكونات المتأثرة تُحدّث
```

#### Test 2: Load Time
```bash
# في Console
performance.mark('start');
// بدّل بين الفترات
performance.mark('end');
performance.measure('toggle-time', 'start', 'end');
```
التوقع: < 50ms

#### Test 3: Memory Leaks
```
1. افتح Chrome DevTools → Memory
2. خذ Heap Snapshot
3. بدّل بين الفترات 20 مرة
4. خذ Heap Snapshot ثاني
5. التوقع: لا يوجد زيادة كبيرة في Memory
```

---

### المرحلة 5️⃣: Deployment

#### الخطوة 1: Build للإنتاج
```bash
npm run build
```

#### الخطوة 2: Test البناء محلياً
```bash
npm run preview
```

#### الخطوة 3: Deploy (مثال: Vercel)
```bash
# إذا كنت تستخدم Vercel
vercel --prod

# أو إذا كنت تستخدم Git deployment
git add .
git commit -m "feat: add annual billing with 2 months free discount"
git push origin main
```

#### الخطوة 4: Smoke Testing في Production
1. افتح الموقع المنشور
2. اختبر المسار الكامل:
   - صفحة Landing → قسم الأسعار
   - Toggle بين شهري/سنوي
   - اختيار باقة
   - صفحة التأكيد
   - إتمام الاشتراك
3. تحقق من قاعدة البيانات Production

---

## 🔍 Checklist النهائي

### قبل Deploy:
- [ ] تم تشغيل Migration على قاعدة البيانات
- [ ] تم اختبار Toggle في صفحة الأسعار
- [ ] تم اختبار Toggle في صفحة التأكيد
- [ ] تم اختبار الحسابات السنوية
- [ ] تم اختبار كود الخصم مع السنوي
- [ ] تم اختبار Dark Mode
- [ ] تم اختبار Responsive Design
- [ ] تم اختبار الاشتراك الفعلي
- [ ] لا يوجد console errors
- [ ] لا يوجد TypeScript errors
- [ ] Build ينجح بدون warnings

### بعد Deploy:
- [ ] الموقع يعمل في Production
- [ ] Toggle يعمل بشكل صحيح
- [ ] الحسابات صحيحة
- [ ] الاشتراكات تُحفظ في قاعدة البيانات
- [ ] لا يوجد performance issues
- [ ] Analytics يعمل (إن وجد)

---

## 🐛 استكشاف الأخطاء

### المشكلة: Toggle لا يعمل
```javascript
// تحقق من:
1. State management صحيح
2. Event handlers مربوطة
3. لا يوجد console errors
```

### المشكلة: الحسابات خاطئة
```javascript
// تحقق من:
1. useMemo dependencies صحيحة
2. الضرب في 10 (ليس 12) للسنوي
3. discount.finalAmount موجود
```

### المشكلة: قاعدة البيانات ترفض الإدراج
```sql
-- تحقق من:
1. Migration تم تشغيله
2. الأعمدة موجودة
3. RLS policies صحيحة
```

### المشكلة: الأسعار لا تتحدث
```javascript
// تحقق من:
1. useEffect dependencies
2. State updates
3. Re-renders
```

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. **تحقق من Console**: افتح DevTools → Console
2. **تحقق من Network**: افتح DevTools → Network
3. **تحقق من Database**: راجع Supabase Logs
4. **راجع الملفات**:
   - `SUBSCRIPTION_IMPROVEMENTS.md` - الشرح التفصيلي
   - `FEATURES_GUIDE.md` - الدليل البصري

---

## 🎉 تم بنجاح!

بعد اتباع هذه الخطوات، يكون لديك:
- ✅ نظام اشتراكات محسّن
- ✅ خيار سنوي بخصم شهرين
- ✅ UI عصري ومحسّن
- ✅ أداء عالي
- ✅ تجربة مستخدم ممتازة

**Happy Coding! 🚀**
