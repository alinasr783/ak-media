# دليل الرسومات البيانية والجداول في Tabibi AI

## نظرة عامة

تم إضافة دعم كامل للرسومات البيانية والجداول باستخدام **Recharts** (أفضل مكتبة React للرسومات).

## المكتبات المثبتة

```bash
npm install recharts @tanstack/react-table
```

## أنواع الرسومات المتاحة

### 1. Line Chart (رسم بياني خطي)

```javascript
{
  type: "recharts",
  chartData: {
    chartType: "line",
    title: "الحجوزات اليومية",
    labels: ["السبت", "الأحد", "الإثنين", "الثلاثاء", "الأربعاء", "الخميس", "الجمعة"],
    datasets: [
      {
        label: "حجوزات العيادة",
        data: [12, 15, 8, 20, 18, 25, 22],
        color: "#3b82f6"
      },
      {
        label: "حجوزات أونلاين",
        data: [5, 8, 6, 10, 12, 15, 18],
        color: "#10b981"
      }
    ]
  }
}
```

### 2. Bar Chart (رسم بياني عمودي)

```javascript
{
  type: "recharts",
  chartData: {
    chartType: "bar",
    title: "إيرادات الأسبوع",
    labels: ["السبت", "الأحد", "الإثنين", "الثلاثاء", "الأربعاء", "الخميس", "الجمعة"],
    datasets: [
      {
        label: "الإيرادات (جنيه)",
        data: [3500, 4200, 2800, 5100, 4800, 6200, 5500],
        color: "#8b5cf6"
      }
    ]
  }
}
```

### 3. Pie Chart (رسم بياني دائري)

```javascript
{
  type: "recharts",
  chartData: {
    chartType: "pie",
    title: "توزيع المرضى حسب الفئة العمرية",
    labels: ["أطفال (0-12)", "شباب (13-30)", "كبار (31-50)", "كبار السن (50+)"],
    datasets: [
      {
        label: "عدد المرضى",
        data: [45, 120, 85, 60]
      }
    ]
  }
}
```

## أنواع الجداول المتاحة

### 1. Simple Table (جدول بسيط)

```javascript
{
  type: "table",
  title: "قائمة المرضى اليوم",
  tableData: {
    headers: ["الاسم", "الموعد", "الحالة", "الهاتف"],
    rows: [
      ["أحمد محمد", "10:00 ص", "حضر", "01012345678"],
      ["فاطمة علي", "11:00 ص", "منتظر", "01098765432"],
      ["محمود حسن", "12:00 م", "ملغي", "01123456789"]
    ]
  }
}
```

### 2. Comparison Table (جدول مقارنة)

مثالي لمقارنة البيانات بين مصدرين أو أكثر (مثل حجوزات العيادة vs حجوزات أونلاين):

```javascript
{
  type: "comparison_table",
  title: "مقارنة الحجوزات - عيادة vs أونلاين",
  data: {
    labels: ["السبت", "الأحد", "الإثنين", "الثلاثاء", "الأربعاء", "الخميس", "الجمعة"],
    datasets: [
      {
        label: "العيادة",
        data: [12, 15, 8, 20, 18, 25, 22]
      },
      {
        label: "أونلاين",
        data: [5, 8, 6, 10, 12, 15, 18]
      }
    ]
  }
}
```

## مثال كامل في Visualization Phase

```javascript
// في aiPipeline.js - visualizationPhase

async visualizationPhase(userMessage, userData, clinicData, subscriptionData) {
  this.setPhase(PHASES.VISUALIZATION);
  
  // مثال: رسم بياني للحجوزات
  const chartData = {
    chartType: "line",
    title: "الحجوزات اليومية - آخر 7 أيام",
    labels: ["السبت", "الأحد", "الإثنين", "الثلاثاء", "الأربعاء", "الخميس", "الجمعة"],
    datasets: [
      {
        label: "حجوزات العيادة",
        data: [12, 15, 8, 20, 18, 25, 22],
        color: "#3b82f6"
      },
      {
        label: "حجوزات أونلاين",
        data: [5, 8, 6, 10, 12, 15, 18],
        color: "#10b981"
      }
    ]
  };
  
  return { type: 'chart', data: chartData };
}
```

## كيفية عرض الرسومات في Building Phase

```javascript
// في buildingPhase - استخدم visualizationData

if (visualizationData?.type === 'chart') {
  // أضف الرسم البياني في الرد
  const response = `
شوف الرسم البياني ده للحجوزات:

[CHART:${JSON.stringify(visualizationData.data)}]

واضح إن الحجوزات الأونلاين بتزيد كل يوم! 📈
  `;
}

// أو جدول مقارنة
if (visualizationData?.type === 'table') {
  const response = `
ده جدول المقارنة:

[COMPARISON_TABLE:${JSON.stringify(visualizationData.data)}]

الحجوزات الأونلاين وصلت 18 حجز الجمعة! 🎉
  `;
}
```

## الألوان المتاحة

```javascript
const COLORS = [
  '#3b82f6',  // blue
  '#10b981',  // green
  '#f59e0b',  // amber
  '#ef4444',  // red
  '#8b5cf6',  // violet
  '#ec4899',  // pink
  '#14b8a6',  // teal
  '#f97316',  // orange
];
```

## ملاحظات مهمة

1. **التحقق من البيانات**: تأكد من وجود `labels` و `datasets` في البيانات
2. **الألوان**: استخدم ألوان مختلفة لكل dataset لسهولة التمييز
3. **العناوين**: أضف عنوان واضح لكل رسم بياني أو جدول
4. **الأنيميشن**: كل الرسومات والجداول لها أنيميشن تلقائي
5. **Responsive**: جميع المكونات responsive وتعمل على الموبايل

## مثال سيناريو كامل

**السؤال**: "عايز أشوف إحصائيات الحجوزات آخر أسبوع"

**Planning Phase**: يحدد أنه محتاج رسم بياني

**Data Fetching Phase**: يجيب بيانات الحجوزات من آخر 7 أيام

**Visualization Phase**: يحضّر رسم بياني خطي + جدول مقارنة

**Building Phase**: يعرض الرسم والجدول مع تحليل بسيط

```javascript
// النتيجة النهائية
{
  text: "تمام! ده تحليل الحجوزات آخر 7 أيام:",
  actions: [
    {
      type: "recharts",
      chartData: {
        chartType: "line",
        title: "الحجوزات اليومية",
        labels: ["السبت", "الأحد", "الإثنين", "الثلاثاء", "الأربعاء", "الخميس", "الجمعة"],
        datasets: [
          { label: "العيادة", data: [12, 15, 8, 20, 18, 25, 22], color: "#3b82f6" },
          { label: "أونلاين", data: [5, 8, 6, 10, 12, 15, 18], color: "#10b981" }
        ]
      }
    },
    {
      type: "comparison_table",
      title: "المقارنة التفصيلية",
      data: {
        labels: ["السبت", "الأحد", "الإثنين", "الثلاثاء", "الأربعاء", "الخميس", "الجمعة"],
        datasets: [
          { label: "العيادة", data: [12, 15, 8, 20, 18, 25, 22] },
          { label: "أونلاين", data: [5, 8, 6, 10, 12, 15, 18] }
        ]
      }
    }
  ]
}
```

## ملفات الكود الرئيسية

- `ChartRenderer.jsx`: كل أنواع الرسومات البيانية
- `TableRenderer.jsx`: كل أنواع الجداول
- `ActionRenderer.jsx`: التكامل مع نظام الـ Actions
- `aiPipeline.js`: Visualization Phase

---

**تم! 🎉** دلوقتي الـ AI عنده القدرة على إنشاء رسومات وجداول احترافية!
